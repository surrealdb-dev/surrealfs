[package]
name = "surrealfs-sandbox"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
surrealfs-types = { workspace = true }

[target.'cfg(target_os = "linux")'.dependencies]
landlock = "0.4"
libc = "0.2"

[dev-dependencies]
tempfile = { workspace = true }
